{
  "name": "stream",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "2.0.3",
    "common-tags": "^1.8.2",
    "form-data": "^4.0.5",
    "node-fetch": "^2.6.1",
    "openai": "^6.27.0",
    "twilio": "^3.84.1"
  },
  "devDependencies": {
    "twilio-run": "^5.0.0"
  },
  "engines": {
    "node": "^20.x || ^22.x"
  }
}
